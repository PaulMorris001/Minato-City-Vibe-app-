diff --git a/node_modules/expo-router/build/native-tabs/NativeBottomTabs/NativeTabsView.js b/node_modules/expo-router/build/native-tabs/NativeBottomTabs/NativeTabsView.js
index 106de95..344ca0b 100644
--- a/node_modules/expo-router/build/native-tabs/NativeBottomTabs/NativeTabsView.js
+++ b/node_modules/expo-router/build/native-tabs/NativeBottomTabs/NativeTabsView.js
@@ -160,6 +160,12 @@ function convertOptionsIconToPropsIcon_4_16(icon) {
         return { sfSymbolName: icon.sf };
     }
     else if ('src' in icon && icon.src) {
+        // PATCH(cityvibe): `original: true` renders the image untinted
+        // (UIImageRenderingModeAlwaysOriginal) instead of as a template —
+        // used for the profile-photo tab icon.
+        if (icon.original) {
+            return { imageSource: icon.src };
+        }
         return { templateSource: icon.src };
     }
     return undefined;
@@ -184,6 +190,17 @@ function convertOptionsIconToPropsIcon_4_18(icon) {
             };
         }
         if ('src' in icon && icon.src) {
+            // PATCH(cityvibe): see convertOptionsIconToPropsIcon_4_16.
+            if (icon.original) {
+                return {
+                    type: 'imageSource',
+                    imageSource: icon.src,
+                    ios: {
+                        type: 'imageSource',
+                        imageSource: icon.src,
+                    },
+                };
+            }
             return {
                 // selectedIcon
                 type: 'templateSource',
