// Base URL of the OurCityvibe backend API.
// Override at build time with VITE_API_URL if needed (e.g. staging).
export const API_BASE = import.meta.env.VITE_API_URL || "https://api.ourcityvibe.com";
