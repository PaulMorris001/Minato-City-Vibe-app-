{
  "name": "nightvibe-root",
  "version": "1.0.0",
  "description": "Root package for NightVibe project",
  "scripts": {
    "mobile": "npm start --prefix mobile",
    "server": "npm run dev --prefix server",
    "admin": "npm run dev --prefix admin",
    "web": "npm run dev --prefix web",

    "build:web": "npm run build --prefix web",
    "build:admin": "npm run build --prefix admin",
    "build:sites": "npm run build:web && npm run build:admin",

    "hash-admin-password": "node server/scripts/hash-admin-password.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "docx": "^9.6.1"
  }
}
